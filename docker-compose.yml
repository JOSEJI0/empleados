version: '3.8'

services:
  mysql:
    image: mysql:9
    container_name: mysql_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root.
      MYSQL_DATABASE: empleado
    ports:
      - "3307:3306"
    volumes:
      - mysqlTspwData:/var/lib/mysql:Z
  app:
    build: .
    container_name: spring_app
    depends_on:
      - mysql
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/empleado
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root.

volumes:
  mysqlTspwData: